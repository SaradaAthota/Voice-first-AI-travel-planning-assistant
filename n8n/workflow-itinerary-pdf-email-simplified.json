{
  "name": "Itinerary PDF Email (Simplified)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "itinerary-pdf",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-trigger",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [250, 300],
      "webhookId": "itinerary-pdf-email"
    },
    {
      "parameters": {
        "jsCode": "// Extract email, city from JSON and convert base64 PDF to binary\nconst input = $input.item.json;\n\n// Extract form fields (now directly in JSON)\nconst email = input.email || '';\nconst city = input.city || 'Unknown City';\nconst duration = input.duration || '';\nconst startDate = input.startDate || '';\n\n// Get base64 PDF data\nlet pdfBase64 = null;\nif (input.pdf && input.pdf.data) {\n  pdfBase64 = input.pdf.data; // Already base64 from backend\n  console.log('PDF base64 data received, length:', pdfBase64.length);\n} else {\n  throw new Error('PDF data not found in input. Input keys: ' + Object.keys(input).join(', '));\n}\n\n// Validate email\nif (!email) {\n  throw new Error('Email not found in webhook input. Input structure: ' + JSON.stringify(Object.keys(input)));\n}\n\n// Return JSON fields and binary PDF (n8n expects base64 string for binary data)\nreturn {\n  json: {\n    email: email,\n    city: city,\n    duration: duration,\n    startDate: startDate\n  },\n  binary: {\n    'itinerary.pdf': {\n      data: pdfBase64, // base64 string (n8n will handle conversion)\n      mimeType: 'application/pdf',\n      fileName: 'itinerary.pdf'\n    }\n  }\n};"
      },
      "id": "extract-and-convert",
      "name": "Extract & Convert PDF",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [450, 300]
    },
    {
      "parameters": {
        "fromEmail": "travel-assistant@example.com",
        "toEmail": "={{ $json.email }}",
        "subject": "Your Travel Itinerary - {{ $json.city }}",
        "emailType": "html",
        "message": "<p>Hello,</p><p>Please find your travel itinerary attached.</p><p>Enjoy your trip!</p>",
        "attachments": {
          "attachments": [
            {
              "name": "itinerary.pdf",
              "dataPropertyName": "itinerary.pdf"
            }
          ]
        },
        "options": {}
      },
      "id": "send-email",
      "name": "Send Email",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2,
      "position": [650, 300],
      "credentials": {
        "smtp": {
          "id": "smtp-credentials",
          "name": "SMTP"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ { \"success\": true, \"message\": \"Itinerary PDF sent to \" + $json.email } }}"
      },
      "id": "respond-to-webhook",
      "name": "Respond to Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [850, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Extract & Convert PDF",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extract & Convert PDF": {
      "main": [
        [
          {
            "node": "Send Email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send Email": {
      "main": [
        [
          {
            "node": "Respond to Webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": [],
  "triggerCount": 1,
  "updatedAt": "2024-01-21T18:00:00.000Z",
  "versionId": "2"
}

